# 安全设计说明 (Security Design)

本平台遵循航天内网安全规范，设计了多层安全防护机制。

## 1. 凭据安全 (Credential Security)
- **零服务端存储**: 用户个人凭据（如第三方系统账号密码）**绝不**上传至服务器，仅加密存储于用户浏览器的 `localStorage`。
- **加密算法**: 使用 Web Crypto API (原生浏览器接口) 进行 AES-GCM 加密。
- **密钥派生**: 加密密钥由用户主密码通过 PBKDF2 算法派生，服务器无法获取密钥。
  - 算法: PBKDF2 (SHA-256, 100,000 次迭代) -> AES-GCM (256位)
- **会话保护**: 解密后的凭据仅在内存中暂存，30分钟无操作自动清除。

## 2. 网络安全 (Network Security)
- **纯内网部署**: 系统无任何外网依赖，禁用 CDN、外部字体、统计脚本。
- **HTTPS**: 建议在生产环境通过 Nginx 反向代理配置 SSL 证书（Docker 容器暴露 HTTP，由网关负责 HTTPS）。
- **CORS/CSRF**: 后端框架内置防护，严格限制跨域请求。

## 3. 审计日志 (Audit Logs)
- 管理员的所有关键操作（审核、配置修改、删除）均记录在 `audit_log` 表中。
- 记录内容包含：操作人、时间、动作详情、IP (需反向代理透传)。

## 4. 数据安全 (Data Security)
- **数据库**: SQLite 单文件存储，易于备份和物理隔离。
- **输入过滤**: 防止 XSS 和 SQL 注入。
- **URL校验**: 提交系统时强制校验内网 IP 段 (10.x, 172.16-31.x, 192.168.x)。

## 5. 部署安全
- 容器化封装，最小权限运行。
- 建议定期备份 `starbase_data` 数据卷。
