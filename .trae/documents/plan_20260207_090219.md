I will fix the three reported issues by improving error handling, refining the UI/UX, and robustifying the option management logic.

### 1. Fix Submission Failure ("Submit Review" fails)
The generic "Submission failed" message hides the actual error (likely a 422 Validation Error or 500 Server Error).
*   **Action**: Update `frontend/src/views/SubmitView.vue` to capture and display the actual error message from the backend response (e.g., `await res.text()`).
*   **Action**: Ensure the `thumbnail_base64` field is handled safely by setting it to `null` if empty, ensuring compatibility with the backend schema.
*   **Action**: Add better validation feedback in the UI.

### 2. Fix Ugly Forms (Publish & Admin)
The forms lack proper spacing, consistent styling, and visual hierarchy.
*   **Action**: Refactor `frontend/src/views/SubmitView.vue` and `frontend/src/views/AdminDashboard.vue` to use improved CSS classes.
    *   Add `gap-4` and `space-y-4` for better spacing.
    *   Style labels with `text-sm font-semibold text-[var(--text-secondary)]`.
    *   Enhance input fields with proper padding, borders, and focus states using standard Tailwind/UnoCSS utilities.
    *   Add a proper "Reset" button or clearer action buttons.

### 3. Fix Option Management (Cannot add more options)
The "Add Option" functionality silently fails if a duplicate name is entered, and the UI doesn't provide feedback.
*   **Action**: Update `frontend/src/views/AdminDashboard.vue`:
    *   Add `alert` or toast notification to show error messages when adding categories/scopes fails (e.g., "Name already exists").
    *   Clear the input fields *only* on success.
    *   Ensure the list refreshes correctly after addition.
    *   Improve the layout of the Option Management section to make it more user-friendly (e.g., separate "Add" card from "List" card, or use a table).

### Execution Steps
1.  **Modify `frontend/src/views/SubmitView.vue`**: Improve error handling and form styling.
2.  **Modify `frontend/src/views/AdminDashboard.vue`**: Improve error handling for categories/scopes and refine form styling.
3.  **Verify**: Ensure submitting a new app works (or shows why it fails) and that adding multiple categories works.
