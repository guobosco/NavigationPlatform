I will implement the edit and resubmission functionality for apps.

### Backend Changes (`backend/main.py`)
1.  **Add `GET /api/apps/{app_id}` endpoint**:
    *   Allows fetching full details of a specific app.
    *   Restricted to the app owner or administrators.
2.  **Add `PUT /api/apps/{app_id}` endpoint**:
    *   Allows updating an existing app.
    *   Restricted to the app owner.
    *   **Logic**: Updates app fields, resets `status` to `0` (Pending), and clears `reject_reason`. This ensures that any edit (whether from an online or rejected app) triggers a new audit request in the admin dashboard.

### Frontend Changes
1.  **User Dashboard (`frontend/src/views/UserDashboard.vue`)**:
    *   Add an **"Edit" button** to each app card in the "My Apps" list.
    *   Clicking this button navigates to the submission page with the app ID (e.g., `/submit?edit=123`).

2.  **Submit Page (`frontend/src/views/SubmitView.vue`)**:
    *   **Edit Mode**: Check for the `edit` query parameter on load.
    *   **Data Loading**: If editing, fetch the existing app data and populate the form.
    *   **Submission Logic**: If in edit mode, send a `PUT` request to update the app instead of a `POST` request to create a new one.
    *   **UI Update**: Change the page title and button text to reflect "Edit" or "Resubmit" context.
